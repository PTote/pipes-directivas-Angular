import { SegundaSeriePipe } from './segunda-serie.pipe';

describe('SegundaSeriePipe', () => {
  it('create an instance', () => {
    const pipe = new SegundaSeriePipe();
    expect(pipe).toBeTruthy();
  });
});
