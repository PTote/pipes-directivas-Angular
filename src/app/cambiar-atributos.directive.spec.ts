import { CambiarAtributosDirective } from './cambiar-atributos.directive';

describe('CambiarAtributosDirective', () => {
  it('should create an instance', () => {
    const directive = new CambiarAtributosDirective();
    expect(directive).toBeTruthy();
  });
});
